<div class="container">
  <h1>Recipes</h1>
    <ul>
      <% @recipes.each do |recipe| %>
        <li>
          <p>
           <%= link_to recipe.title ,recipe_path(recipe) %>
          </p>
        </li>
      <% end%>
    </ul>
    <%= link_to "Create Recipe", new_recipe_path %>
</div>